<?xml version="1.0" encoding="utf-8"?>
<!--
  accessibility_service_config.xml — 无障碍服务配置
  ──────────────────────────────────────────────────────────────
  对应文件：GameDetectAccessibilityService.kt
  存放路径（原生项目中）：app/src/main/res/xml/accessibility_service_config.xml

  说明：
  - packageNames 留空，监听所有 App 的窗口切换事件
    （若只监听特定游戏，可在此添加包名，格式见注释）
  - 仅订阅 TYPE_WINDOW_STATE_CHANGED，资源占用最小
  - canRetrieveWindowContent = false：不读取窗口内容，保护用户隐私
  ──────────────────────────────────────────────────────────────
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity=""
    />
    <!--
    若需限定监听包名，取消注释并填入游戏包名（多个用逗号分隔）：
    android:packageNames="com.example.game1,com.example.game2"
    -->
